# ============================================
# Help-Desk Bot — Environment Configuration
# ============================================
# Copy this file to .env (for dev) or .env.production (for Docker)
# and fill in your actual values.

# ---- Telegram Bot ----
# Get this from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
BOT_NAME=Helpdesk Bot

# ---- Webhook ----
# Your public HTTPS URL (ngrok for dev, real domain for prod)
WEBHOOK_URL=https://your-ngrok-url.ngrok-free.dev
PORT=3000

# Pin the webhook secret so the path survives container restarts.
# Leave empty on first run, then copy the generated value from logs.
WEBHOOK_SECRET=

# ---- PostgreSQL ----
DB_HOST=db
DB_PORT=5432
DB_NAME=helpdesk
DB_USER=helpdesk
DB_PASSWORD=change-me-to-a-strong-password

# Required by the postgres:15 Docker image (must match above)
POSTGRES_DB=helpdesk
POSTGRES_USER=helpdesk
POSTGRES_PASSWORD=change-me-to-a-strong-password

# ---- Branch Support Groups (Telegram group chat IDs) ----
# Negative numbers like -1001234567890
# See README.md > "Adding Support Groups / Staff" for how to get these
SUPPORT_GROUP_ID=
SUPPORT_GROUP_JHQ=
SUPPORT_GROUP_TRK=
SUPPORT_GROUP_GS=
SUPPORT_GROUP_IPIL=

# ---- Central Monitoring (optional) ----
CENTRAL_MONITORING_GROUP=
ENABLE_CENTRAL_MONITORING=false

# ---- Authorization ----
# Comma-separated Telegram numeric user IDs
# See README.md > "Adding Support Groups / Staff" for how to get these
SUPPORT_STAFF_IDS=
EMPLOYEE_IDS=

# ---- Scheduled Reports ----
DAILY_REPORT_HOUR=18
DAILY_REPORT_MINUTE=0

# ---- Logging ----
NODE_ENV=production
LOG_LEVEL=info
LOG_DIR=/app/logs
